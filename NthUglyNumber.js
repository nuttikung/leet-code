/**
 * @param {number} n
 * @return {number}
 */
var nthUglyNumber = function (n) {
	// if (n <= 5) {
	// 	return n;
	// }
	// let temp = [1];
	// let i = 1;
	// // while (i < n * 2) {
	// // 	temp.push(2 * i);
	// // 	temp.push(3 * i);
	// // 	temp.push(5 * i);
	// // 	temp = temp.sort((a, b) => a - b);
	// // 	temp = [...new Set(temp)];
	// // 	i += 1;
	// // }
	// return temp[n - 1];
};